name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: ci/build-app.sh
    container: gradle:6-jdk11
      - name: Clone-down
        uses: actions/checkout@v2
         